# InterviewAI Backend - Environment Variables
# Copy this file to .env and fill in the values

# =============================================================================
# D-ID API Configuration (Required for video avatar)
# =============================================================================
# Get your API key from: https://studio.d-id.com/account
# The key should be Base64 encoded: base64(email:password)
DID_API_KEY=your_did_api_key_here

# =============================================================================
# Optional Configuration
# =============================================================================

# Avatar mode: "video" (uses D-ID) or "audio_only" (fallback)
# Set to "audio_only" if D-ID credits are exhausted
AVATAR_MODE=video

# Server configuration
PORT=8000
HOST=0.0.0.0

# Debug mode (set to false in production)
DEBUG=false
# Supabase PostgreSQL Database Connection
# Option 1: Use DATABASE_URL (recommended - copy from Supabase dashboard > Project Settings > Database > Connection string)
SUPABASE_DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
